# Copiar este archivo a .env.local y completar con los valores que imprime
# `supabase start` (corrida desde la raíz del repo, no desde /frontend).
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
